"use strict";
/*
 * Copyright (c) Testmo GmbH (Berlin, Germany)
 * All rights reserved.
 * contact@testmo.com - www.testmo.com
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.richFormatHelp = void 0;
var chalk_1 = __importDefault(require("chalk"));
function richFormatHelp(ansi, text) {
    if (ansi) {
        // Find and highlight section titles, usage etc.
        var lines = text.split('\n');
        var titleRegex = /^[a-zA-Z0-9]{1,20}:$/i;
        var usageRegex = /^Usage:/i;
        for (var i = 0; i < lines.length; i++) {
            var line = lines[i];
            if (usageRegex.test(line)) {
                lines[i] = chalk_1.default.bold.yellow(line);
            }
            else if (titleRegex.test(line)) {
                lines[i] = chalk_1.default.bold.underline(line);
            }
        }
        return lines.join('\n');
    }
    else {
        return text;
    }
}
exports.richFormatHelp = richFormatHelp;

"use strict";
/*
 * Copyright (c) Testmo GmbH (Berlin, Germany)
 * All rights reserved.
 * contact@testmo.com - www.testmo.com
 */
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.resolveFiles = void 0;
var glob_1 = __importDefault(require("glob"));
// Uses glob to find find files for the specified patterns, but only if/
// it suspects that the passed patterns are not already a list of resolvef iles
function resolveFiles(patterns) {
    if (patterns.length != 1) {
        return patterns;
    }
    if (!glob_1.default.hasMagic(patterns[0])) {
        return patterns;
    }
    var files = glob_1.default.sync(patterns[0]);
    return files;
}
exports.resolveFiles = resolveFiles;
